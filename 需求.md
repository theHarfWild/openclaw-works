自动论文生成 Agent (基于 OpenClaw) 需求与开发文档
1. 项目概述
本项目旨在基于 OpenClaw 框架进行二次开发，构建一个具备极高透明度（白盒化）的自动论文写作智能体。该智能体能够通过多轮对话理解用户意图，自主进行文献检索、大纲规划、章节撰写、实验执行及数据可视化，并最终通过本地 LaTeX 引擎编译生成 PDF。整个工作流在指定的工作区内增量完成，确保用户可以随时干预和查看中间态（.tex 文件）。

2. 核心工作流 (Workflow)
系统状态遵循以下流转机制：

意图触发 (Intent Recognition): 识别出用户对话中的“论文写作”意图，路由至 PaperWriting_Skill。

需求对齐 (Requirement Gathering): * 检查是否包含核心实体（主题 Topic）。缺失则反问。

引导用户明确论文格式（如 IEEE, Springer 等）和章节结构（如 Abstract, Intro, Method, Exp, Conclusion）。

资料检索与大纲生成 (Research & Outlining): 智能体联网检索相关文献和前沿资料，生成整体写作思路和框架。

增量撰写 (Incremental Drafting): 逐个章节生成内容，并以 .tex 格式写入统一的默认工作目录。

实验沙盒执行 (Experiment Execution): 如果章节涉及实验，智能体需生成相应的可执行代码（如 Python），在安全的沙盒环境（如 Conda 虚拟环境）中运行，收集输出结果（图表、数据），并将其组织到 .tex 文件中。

编译与展示 (Compilation & Delivery): * 完成所有 .tex 撰写后，调用本地编译命令（如 latexmk 或 pdflatex）生成 PDF。

通过后端接口将 PDF 文件流/路径传递给前端。

反馈与修改 (Revision Loop): 用户提出修改意见，智能体在原有工作目录的同一文件中进行精准修改并重新编译。

3. 目录与工作区管理规范
所有过程必须在用户专属的默认工作目录中进行，禁止乱序创建多余文件夹。标准结构如下：

Plaintext
workspace/paper_task_{task_id}/
├── main.tex              # 唯一的主 Tex 文件，所有修改都在此进行
├── references.bib        # 参考文献库
├── figures/              # 存放检索到的或实验生成的图片
├── exp_sandbox/          # 智能体执行实验代码的目录
│   ├── run_exp.py        # 智能体生成的实验脚本
│   └── exp_logs.txt      # 实验运行日志
└── output.pdf            # 最终编译产物，供前端读取
4. 详细模块设计
4.1 意图路由与多轮对话模块
功能: 拦截用户输入，正则或 LLM 判别触发 PaperWriting_Skill。

状态机设计: 维护一个 State 对象，记录当前处于 [等待主题] -> [等待格式] -> [等待结构] -> [就绪] 中的哪一个阶段。

4.2 Web Agent 检索与大纲模块
功能: 使用 Search Tool 获取领域背景知识。

开发要求: 必须要求 LLM 给出带有逻辑链条的 Markdown 大纲，经用户（可选）确认后，转化为 .tex 的骨架（包含 \section{} 等占位符）。

4.3 实验自组织与执行模块 (核心难点)
功能: 根据论文 Method 部分，构建验证实验。

开发要求:

代码生成: 智能体需编写包含环境依赖声明的 Python 脚本。

沙盒执行: 后端需提供一个 Python subprocess 执行接口，限制执行时间。

结果回收: 智能体必须被 Prompt 约束，将实验图片固定输出到 figures/ 目录下，并在 .tex 中使用 \includegraphics{} 正确引用。

4.4 文件系统操作模块 (FS Toolkit)
功能: 提供精准的文件读写能力。

开发要求: * 放弃简单的整体覆写，开发 Read_Section 和 Replace_Section 工具，允许智能体基于正则表达式或抽象语法树（AST）对 main.tex 的特定章节进行局部修改，保证上下文稳定。

4.5 编译与前后端交互模块
编译工具: 封装 subprocess.run(["latexmk", "-pdf", "main.tex"], cwd=workspace_path)。需捕获标准错误（stderr），若编译失败，将错误日志传回给智能体让其自行 debug 并修改 .tex。

前端展示: 提供一个独立的 API EndPoint（如 /api/get_pdf?task_id=xxx），前端使用 PDF.js 或 iframe 直接渲染展示。

(提示：目前我在windows系统上没有tex编译工具，但是我的linux上有，所以你在开发时不必验证是否能够成功调用tex，只需要可以调用即可)